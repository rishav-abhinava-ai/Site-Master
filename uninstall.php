<?php
/**
 * Site Master uninstall entry point.
 *
 * Site Master performs no destructive cleanup unless a future, explicitly
 * approved uninstall policy is implemented.
 *
 * @package SiteMaster
 */

defined( 'WP_UNINSTALL_PLUGIN' ) || exit;

/*
 * Intentionally no-op.
 * Any future destructive cleanup must be explicitly designed, user-authorized,
 * documented in PRODUCTION_HANDOFF.md, and protected by the genuine WordPress
 * uninstall lifecycle plus an explicit delete-data setting.
 */
