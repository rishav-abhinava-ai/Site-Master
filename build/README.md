# Build

Development 0.0.2 contains only the build boundary.

The deterministic development/production packaging implementation is scheduled for a later authorized development phase.

Production packaging must follow `AGENTS.md` and `PRODUCTION_HANDOFF.md` and must use a clean temporary staging copy.
