# Site Master Content Analytics

Reserved shared module. Runtime implementation is scheduled for Development `0.1.0`.

Development `0.0.2` must not create analytics tables, collectors, tracking assets, dashboard pages, cron jobs, migrations, or counters. See `docs/CONTENT-ANALYTICS-SPEC.md`.
